import styled from "styled-components";

export const Top = () => {
  return (
    <SContaioner>
      <h2>Top Page </h2>
    </SContaioner>
  );
};

const SContaioner = styled.div`
  text-align: center;
`;
